import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      dark: 'hsla(175, 61%, 20%, 1)',
      main: 'hsla(175, 61%, 40%, 1)',
      light: 'hsla(175, 61%, 75%, 1)',
      contrastText: 'hsla(210, 17%, 98%, 1)',
    },
  },
});
